#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Action {
    Tick,
    Quit,
    NextScreen,
    PrevScreen,
    MoveLeft,
    MoveRight,
    MoveUp,
    MoveDown,
    ExtendPadSelection(isize, isize),
    AdjustSelected(i32),
    ActivateSelected,
    RefreshDevices,
    ConnectSelected,
    Disconnect,
    RefreshConfig,
    ShowPacketLog,
    ToggleApplyModal,
    ConfirmApply,
    CancelModal,
    TogglePadSelection,
    TogglePadBank,
    ClearPadSelection,
    UndoField,
    DiscardAll,
    ShowHelp,
}
